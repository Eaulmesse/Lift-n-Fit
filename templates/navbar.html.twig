{% block body %}


<nav class="navbar navbar-expand-lg navbar-light bg-light d-flex flex-column">
    <div class="container-fluid align-items-start d-flex justify-content-between">
        <div class="navbar-title">
            <a class="navbar-brand fs-1 bungee text-success" href="{{ path('app_home') }}">Lift'N'Fit</a>
        </div>
 
    
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
                <ul class="navbar-nav text-end">
                    {% if is_granted('ROLE_USER') %}
                        <li class="nav-item">
                            <a class="nav-link lato link-success fs-5" href="{{ path('app_logout') }}">Deconnexion</a> 
                        </li>
                    {% else %}
                        <li class="nav-item">
                        <a class="nav-link lato link-success fs-5" href="{{ path('app_login') }}">Connexion</a>
                        </li>
                        <li class="nav-item">
                        <a class="nav-link lato link-success fs-5" href="{{ path('app_register') }}">Créer son compte</a>
                        </li>
                    {% endif %}
                    <li class="nav-item mobile-only">
                        <a class="nav-link lato link-success fs-5" href="{{ path('app_conseil_all')}}">Conseil</a>
                    </li>
                    <li class="nav-item mobile-only">
                        <a class="nav-link lato link-success fs-5" href="{{ path('app_post_all')}}">Forum</a>
                    </li>
                    {% if is_granted('ROLE_USER') %}
                    <li class="nav-item mobile-only">
                        <a class="nav-link lato link-success fs-5" href="{{ path('app_user_account')}}">Mon compte</a>
                    </li>
                    {% endif %}
                    

                 </ul>
            </div>
        
    </div>

    <div class="container-fluid d-flex justify-content-between bg-success p-3">

        <div class="left-side">
            <div class="collapse navbar-collapse desktop-only " id="navbarSupportedContent">
                <ul class="navbar-nav text-end">
                            
                    <li class="nav-item">
                        <a class="nav-link lato link-light fs-5" href="{{ path('app_conseil_all')}}">Conseil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link lato link-light fs-5" href="{{ path('app_post_all')}}">Forum</a>
                    </li>

                </ul>
            </div>
        </div>
        

        <div class="right-side">
            <div class="collapse navbar-collapse desktop-only " id="navbarSupportedContent">
                <ul class="navbar-nav text-end">
                    {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link lato link-light fs-5" href="{{ path('app_user_account')}}">Mon compte</a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </div>
</nav>

{% endblock %}
